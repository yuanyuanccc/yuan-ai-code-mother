spring:
  application:
    name: yuan-ai-code-mother-backend
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/yuan_ai_code_mother
    username: root
    password: 123456
server:
  port: 8123
  servlet:
    context-path: /api

springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan:
        - com.yuanc.yuanaicodemother.controller
knife4j:
  enable: true
  setting:
    language: zh_cn